# Shortcuts exports (optional)

Place an exported **`Scrimp: Log from Wallet.shortcut`** here for version control backup.

See [docs/shortcuts-wallet.md](../docs/shortcuts-wallet.md) for how to create and publish the shortcut. The app installs via the iCloud link configured in `.env` / `app.json`, not from this folder by default.
